type User {
  id: ID!
  email: String
  displayName: String
  isActive: Boolean
  role: String
  createdAt: String
}

type Query {
  pendingUsers: [User!]!
}

mutation {
  approveUser(id: ID!): User!
}